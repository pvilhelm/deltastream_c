
#include <stdlib.h>

#include "node_tree.h"

static int test_node_add() {
    tree_node_t root = {0};
    node_t* n = calloc(1,sizeof(node_t));
    //% add_child(&root, 0) == 0
    //% add_child(0, n) == 0
    
    root.n = n;
    n->ip = 10;
    n->port = 5;
    
    node_t* tmp = calloc(1,sizeof(node_t));
    tmp->ip = 20;
    tmp->port = 10;

    add_child(&root,tmp);
    
    
    return 0; 
}